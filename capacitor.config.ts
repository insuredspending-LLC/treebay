import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.insuredspending.treebay',
  appName: 'Tree Marketplace',
  webDir: 'dist'
};

export default config;
